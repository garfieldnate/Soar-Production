sp {hello-world
	"some documentation"
	:i-support
	:o-support
   (state <s> ^type state)
   -{
	(<s> ^foo)
	(<s> ^bar)
   }
-->
   (<s> ^result failure
        ^sub-phase result -
        ^sub-phase notify)
   (write |  expecting - | <entry1-name> | (| <entry1-s-name> |)| (crlf))
   (write |  result - query failure| (crlf))
}